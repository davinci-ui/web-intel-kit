# SearXNG Settings
# See: https://docs.searxng.org/admin/settings/index.html

use_default_settings: true

server:
  secret_key: "change-me-to-something-random"
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false  # Disable rate limiting for local/agent use

search:
  safe_search: 0
  autocomplete: "google"
  default_lang: "auto"
  formats:
    - html
    - json  # Required for API access

ui:
  static_use_hash: true
  default_theme: simple

engines:
  # Ensure JSON format is available
  - name: google
    engine: google
    shortcut: g
    disabled: false

  - name: bing
    engine: bing
    shortcut: b
    disabled: false

  - name: duckduckgo
    engine: duckduckgo
    shortcut: ddg
    disabled: false

  - name: wikipedia
    engine: wikipedia
    shortcut: wp
    disabled: false
